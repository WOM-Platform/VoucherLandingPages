<p>{{{ i18n "migration-description" }}}</p>

<div class="buttons are-large is-centered call-to-action">
    <a href="{{womProtocol}}://migration/{{otc}}{{#if password}}/{{password}}{{/if}}" title="{{ i18n "migration-button-tooltip" }}" class="button is-info">{{{ i18n "migration-button-text" }}}</a>
</div>
